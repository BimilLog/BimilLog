# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official

# Enable 16KB page size support (Android 15+)
android.experimental.enableSupportForPageSize16KB=true

# Suppress compileSdk warning
android.suppressUnsupportedCompileSdk=35

# Signing config (DO NOT commit these values to git)
# Create a local.properties file and add these values there instead
# RELEASE_STORE_PASSWORD=your_keystore_password
# RELEASE_KEY_PASSWORD=your_key_password
